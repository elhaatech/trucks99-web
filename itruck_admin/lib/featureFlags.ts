/**
 * Central feature-flag toggles for admin-portal modules.
 *
 * Flip SHOW_ADVERTISEMENT_MODULE to true to re-enable the Advertisement
 * module (sidebar link + routes). All Advertisement code stays intact;
 * this flag only controls visibility and route access.
 */
export const SHOW_ADVERTISEMENT_MODULE = false;
